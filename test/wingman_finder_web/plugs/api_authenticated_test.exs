defmodule WingmanFinderWeb.Plug.ApiAuthenticatedTest do
  use WingmanFinderWeb.ConnCase

  describe "init/1" do
    test "assert false", %{conn: _conn} do
      assert false
    end
  end

  describe "call/2" do
    test "assert false", %{conn: _conn} do
      assert false
    end
  end
end
